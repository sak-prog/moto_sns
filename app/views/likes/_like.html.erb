<% if user_signed_in? %>
  <hr>
  <% if current_user.already_liked?(post) %>
    <%= button_to 'いいねを取り消す', post_like_path(post), method: :delete,class: "btn btn-default" %>
  <% else %>
    <%= button_to 'いいね', post_likes_path(post),class: "btn btn-primary" %>
  <% end %>
<% end %>